<div class="media text-muted pt-3">
    {% set initials = post.user.fullName|split(' ') %}
    <img data-src="holder.js/32x32?text={% if initials[0] is defined and initials[0] is not empty %}{{ initials[0]|first }}{% if initials[1] is defined %}{{ initials[1]|first }}{% endif %}{% else %}{{ post.user.username|first }}{% endif %}&bg=e83e8c&fg=fff&size=8" alt="" class="mr-2 rounded">
    <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        <span class="d-block">
            <a href="{{ path('micro_post_user', {'username': post.user.username}) }}"><strong class="text-gray-dark">@{{ post.user.username }}</strong></a>
            <small>· {{ post.time|date("d. m. Y H:i") }}</small>
        </span>
        <a href="{{ path('micro_post_show', {'id': post.id}) }}">
            {{ post.text }}
        </a>
    </p>
</div>